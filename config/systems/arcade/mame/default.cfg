<?xml version="1.0"?>
<mameconfig version="10">
    <system name="default">
        <input>
            <!-- Avoid joystick controllers cause lr-mame to go back to the MAME launcher menu -->
            <port type="UI_CANCEL">
                <newseq type="standard">
                    KEYCODE_ESC
                </newseq>
            </port>
        </input>
    </system>
</mameconfig>

# Disable 4k output to improve performance
hdmi_max_pixel_freq=200000000

# Disable overscan (causes black bars around screen otherwise)
disable_overscan=1

# Switch TV to Game Mode when possible
edid_content_type=4

# Fix lag on cheap mice
usbhid.mousepoll=0

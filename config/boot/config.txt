# Fix HDMI settings being misinterpreted, e.g. when using an Argon1 case
#hdmi_force_hotplug=1
#hdmi_group=1
#hdmi_mode=16
hdmi_ignore_edid=0xa5000080

# Disable 4k output to improve performance
hdmi_max_pixel_freq=200000000

# Disable overscan (causes black bars around screen otherwise)
disable_overscan=1

# Switch TV to Game Mode when possible
edid_content_type=4

# Fix lag on cheap mice
usbhid.mousepoll=0

[pi4]
# Overclocking
over_voltage=2
arm_freq=1750

[all]

# Disable wifi (here to avoid conflicts with the other dtoverlay)
dtoverlay=disable-wifi
